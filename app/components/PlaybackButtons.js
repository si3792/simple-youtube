import React from 'react';

const PlaybackButtons = (playback) => (
  <div>
    <button>Play</button>
    <button>Pause</button>
    <button>Stop</button>
  </div>
);

export default PlaybackButtons;
